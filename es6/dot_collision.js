export default function(state){
  // get the x y of selected dot
  // get the x y of dots with the same pairing as selected dots
  // check for collision
  // update states accordingly
  return state;
};
